import AbsencesTable from '@components/AbsencesTable';
import React, { FC } from 'react';

const HomePage: FC = () => {
  return (
    <div>
      <AbsencesTable />
    </div>
  );
};

export default HomePage;
